;;;;;;;;;; Info ;;;;;;;;;;
;; Swapping default 'ctrl' and 'meta' to 'fn' and 'command'
;; Only runs on MacOS
;; (refer to ~/.emacs file under 'tweaks')

;; Change ctrl to fn key
(setq ns-function-modifier 'control)

;; Change meta to command key
(setq ns-command-modifier 'meta)
